<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet"  href="./CSS/style.css">
</head>
<body>
    <div class="container">
        <div class="container2">
        <h1 class="head">Signup page</h1>
        <form action="/signup" method="post">

            <input type="text" required name="name" placeholder="Enter Name">
            <div>
                <input type="text" required name="email" placeholder="Email">
                <% if (locals.email) { %>
                    <p  > <%= locals.email %></p>
                <% } %>
            </div>

            
            <input type="password" required name="password"  placeholder="password">
            <input type="password" required name="retype_password"  placeholder="Retype password">
            <% if (locals.password_mismatch) { %>
                <p > <%= locals.password_mismatch %></p>
            <% } %>
            
            <div class="container1">
                <button  class="button" type="submit">Signup</button>
            </div>
            <div class="w-3 h-12">
                <a href="http://localhost:4000/">Login</a>
            </div>
        </form>
        
    </div>
    </div>
</body>
</html>